{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commentaire list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Auteur</th>
                <th>Date</th>
                <th>Contenu</th>
                <th>Note</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commentaire in commentaires %}
            <tr>
                <td><a href="{{ path('Catcommentaire_show', { 'id': commentaire.id }) }}">{{ commentaire.id }}</a></td>
                <td>{{ commentaire.auteur }}</td>
                <td>{% if commentaire.date %}{{ commentaire.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ commentaire.contenu }}</td>
                <td>{{ commentaire.note }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('catalogue_commentaire_show', { 'id': commentaire.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('catalogue_commentaire_edit', { 'id': commentaire.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('catalogue_commentaire_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
